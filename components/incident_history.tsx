'use client'
import { Download } from 'lucide-react'
import type { Incident } from '@/lib/incident_engine'
export function IncidentHistory({incidents}:{incidents:Incident[]}){const exportCsv=()=>{const csv='equipo,causa,mttd,mttr,estado\n'+incidents.map(i=>`${i.equipoId},${i.causa},${i.mttd},${i.mttr},${i.estado}`).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='netvista-incidencias.csv';a.click()};return <div className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-bold">Historial de incidencias</h2><p className="mt-1 text-xs text-muted-foreground">Registro de eventos de la sesión</p></div><button onClick={exportCsv} className="flex items-center gap-2 rounded-lg border border-border px-3 py-2 text-xs font-bold"><Download className="h-4 w-4"/>Exportar CSV</button></div><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="text-muted-foreground"><tr><th className="px-5 py-3 font-medium">Equipo</th><th className="px-3 py-3 font-medium">Causa</th><th className="px-3 py-3 font-medium">MTTD</th><th className="px-3 py-3 font-medium">MTTR</th><th className="px-5 py-3 font-medium">Estado</th></tr></thead><tbody>{incidents.length?incidents.map(i=><tr key={i.id} className="border-t border-border"><td className="px-5 py-3 font-semibold">{i.equipoId}</td><td className="px-3 py-3 text-muted-foreground">{i.causa}</td><td className="px-3 py-3">{i.mttd} min</td><td className="px-3 py-3">{i.mttr} min</td><td className="px-5 py-3 text-emerald-400">{i.estado}</td></tr>):<tr><td colSpan={5} className="px-5 py-10 text-center text-muted-foreground">Aún no hay incidencias resueltas.</td></tr>}</tbody></table></div></div>}
