'use client'
import { AlertTriangle, CheckCircle2, Play, ShieldAlert, Timer } from 'lucide-react'
import type { Alert } from '@/lib/incident_engine'
export function AlertCenter({alerts,onSimulate,onDiagnose,onResolve}:{alerts:Alert[];onSimulate:()=>void;onDiagnose:(id:string)=>void;onResolve:(id:string)=>void}){return <div className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-bold">Centro de alertas</h2><p className="mt-1 text-xs text-muted-foreground">Ciclo de vida de incidencias</p></div><button onClick={onSimulate} className="flex items-center gap-2 rounded-lg bg-red-500/10 px-3 py-2 text-xs font-bold text-red-400"><ShieldAlert className="h-4 w-4"/>Simular falla</button></div><div className="space-y-3 p-5">{!alerts.length?<div className="py-8 text-center text-sm text-muted-foreground"><CheckCircle2 className="mx-auto mb-2 h-8 w-8 text-emerald-400"/>No hay alertas activas. La red está estable.</div>:alerts.map(a=><div key={a.id} className="rounded-lg border border-border p-4"><div className="flex gap-3"><AlertTriangle className={`h-4 w-4 ${a.severidad==='critical'?'text-red-400':'text-amber-400'}`}/><div className="flex-1"><div className="flex justify-between"><p className="text-sm font-semibold">{a.equipoNombre}</p><span className="text-[10px] font-bold uppercase text-red-400">{a.severidad}</span></div><p className="mt-1 text-xs text-muted-foreground">{a.mensaje}</p><p className="mt-2 flex items-center gap-1 text-[11px] text-muted-foreground"><Timer className="h-3 w-3"/>Detectada {new Date(a.timestamp).toLocaleTimeString('es-MX',{hour:'2-digit',minute:'2-digit'})}</p><div className="mt-3 flex gap-2">{a.status==='activa'&&<button onClick={()=>onDiagnose(a.id)} className="flex items-center gap-1 rounded-md bg-primary px-2.5 py-1.5 text-[11px] font-bold text-primary-foreground"><Play className="h-3 w-3"/>Iniciar diagnóstico</button>}{a.status==='diagnostico'&&<button onClick={()=>onResolve(a.id)} className="rounded-md bg-emerald-500/15 px-2.5 py-1.5 text-[11px] font-bold text-emerald-400">Marcar resuelta</button>}<span className="rounded-md border border-border px-2.5 py-1.5 text-[11px] text-muted-foreground">{a.status}</span></div></div></div></div>)}</div></div>}
export function AlertRules(){return <div className="rounded-xl border border-border bg-card p-5"><h2 className="font-bold">Reglas de alerta</h2><p className="mt-1 text-xs text-muted-foreground">Define cuándo debe intervenir el NOC.</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs text-muted-foreground">Alertar si latencia &gt;<input defaultValue="80" type="number" className="mt-2 w-full rounded-lg border border-border bg-muted/20 px-3 py-2 text-sm outline-none"/></label><label className="text-xs text-muted-foreground">Alertar si jitter &gt;<input defaultValue="30" type="number" className="mt-2 w-full rounded-lg border border-border bg-muted/20 px-3 py-2 text-sm outline-none"/></label></div><button className="mt-4 rounded-lg border border-primary/40 px-3 py-2 text-xs font-bold text-primary">Guardar reglas</button></div>}
