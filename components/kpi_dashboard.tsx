'use client'
import { Activity, Gauge, TrendingDown } from 'lucide-react'
export function KpiDashboard({mttd,mttr,incidents}:{mttd:number;mttr:number;incidents:number[]}){return <div className="rounded-xl border border-border bg-card p-5"><div className="flex items-center justify-between"><div><h2 className="font-bold">Dashboard de KPIs</h2><p className="mt-1 text-xs text-muted-foreground">Rendimiento operativo de la sesión</p></div><Gauge className="h-5 w-5 text-primary"/></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><Kpi label="MTTD promedio" value={`${mttd||47} min`} base="Base 47 min" icon={<Activity/>}/><Kpi label="MTTR promedio" value={`${mttr||124} min`} base="Base 124 min" icon={<TrendingDown/>}/><Kpi label="Incidencias" value={String(incidents.length)} base="En esta sesión" icon={<Gauge/>}/></div><div className="mt-5 h-24 rounded-lg bg-muted/20 p-3"><div className="flex h-full items-end gap-2">{[35,55,42,70,48,80,60,75,50,68,45,82].map((v,i)=><div key={i} className="flex-1 rounded-t bg-primary/70" style={{height:`${v}%`}}/>)}</div></div></div>}
function Kpi({label,value,base,icon}:{label:string;value:string;base:string;icon:React.ReactNode}){return <div className="rounded-lg border border-border p-4"><div className="flex items-center justify-between text-primary">{icon}<span className="text-[10px] text-muted-foreground">{base}</span></div><p className="mt-3 text-2xl font-black tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{label}</p></div>}
